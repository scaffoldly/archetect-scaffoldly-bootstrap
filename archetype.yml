---
script:
  - if:
      conditions:
        - switch-enabled: overwrite
      then:
        - rules:
            - destination:
                overwrite: true

  - set:
      nonlive_domain:
        prompt: "Nonlive Domain"
      live_domain:
        prompt: "Live Domain"

  - render:
      directory:
        source: "contents"
