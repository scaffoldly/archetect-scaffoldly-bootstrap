---
script:
  - if:
      conditions:
        - switch-enabled: overwrite
      then:
        - rules:
            - destination:
                overwrite: true

  - set:
      nonlive_domain:
        default: scaffoldly.dev
      live_domain:
        default: scaffoldly.live
  - render:
      directory:
        source: "contents"
